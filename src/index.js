import React from 'react';
import ReactDOM from 'react-dom';
import '../src/Styles/styles.css'; // Importamos los estilos globales
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
